{% extends '::admin.html.twig' %}

{% block body -%}
    <h1>Wedding creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_wedding') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
